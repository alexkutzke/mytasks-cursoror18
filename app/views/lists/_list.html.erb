<h4><%= list.name %> <small><%= link_to "Editar", edit_list_path(list)%></small></h4> 
<% if list.tasks.empty? %>
	<p><strong>Nenhuma tarefa nessa lista! :)</strong></p>
<% else %>
	<table>
		<%= render list.tasks %>
	</table>
<% end %>
